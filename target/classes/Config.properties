headlessMode=false

screenshotMode=always
explicitTimeOut=2

# MUI component timeout (seconds)
mui.timeout=10

# PrimeNG component timeout (seconds)
primeng.timeout=10

# Execution identity (optional - leave blank to auto-detect from OS)
execution.user=
execution.machine=